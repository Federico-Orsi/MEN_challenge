# New User

components:
  requestBodies:
    newUser:
      type: object
      required:
        - email
        - password
      properties:
        email:
          type: string
          description: El email del nuevo usuario a registrar.
        password:
          type: string
          description: La contraseña del nuevo usuario.

      example:
        email: blablabla@gmail.com
        password: "12345"
